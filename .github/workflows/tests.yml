name: tests
on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker-compose up --build -d
      - run: docker-compose exec -T back npm run test
